<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.horse.dao.ReviewDao">
	<!-- 리뷰입력 -->
	<insert id="reviewinsert" parameterType="reviewvo">
		insert into review
		values(review_seq.nextVal,#{retitle},#{recontent},0,sysdate)
	</insert>


	<!-- 리뷰리스트 -->
	<select id="reviewList" resultType="reviewvo">
		select r_num, renum, retitle,
		recontent,
		rehit, redate
		from (SELECT ROWNUM
		r_num, a.* FROM
		(SELECT *
		FROM review ORDER BY 1 DESC
		) a
		)
	</select>

	<!-- 상세보기 -->
	<select id="reviewDetail" parameterType="int"
		resultType="reviewvo">
		select
		renum, retitle, recontent, rehit, redate from review
		where renum=#{renum}
	</select>



	<!-- 수정 -->
	<update id="reviewUpdate" parameterType="reviewvo">
		update review set
		retitle=#{retitle}, recontent=#{recontent} where renum=#{renum}
	</update>


	<!-- 삭제 -->
	<delete id="reviewDelete" parameterType="int">
		delete from review where
		renum=#{renum}
	</delete>

	<!-- 조회수 -->
	<update id="reviewHit" parameterType="int">
		update review set rehit =
		rehit + 1 where renum=#{renum}
	</update>

	<!-- 리뷰 테이블 갯수 -->
	<select id="reviewCount" resultType="int" parameterType="svo">
		select count(*) cnt from review
		<if test="searchValue != null">
			<where>
				<choose>
					<when test="searchType ==1">
						writer Like '%' || #{searchValue} || '%'
					</when>
					<when test="searchType == 2">
						grpcode Like'%' || #{searchValue} || '%'
					</when>
					<when test="searchType==3">
						comm Like '%' || #{searchValue} || '%'
					</when>
				</choose>
			</where>
		</if>
	</select>
	
	<!-- 리뷰 페이징 리스트 -->
	<select id="reviewListPage" resultType="reviewvo" parameterType="svo">
		select * from(select rownum r_num, a.* from
		(select * from review 
		<if test="searchValue != null">
			<where>
				<choose>
					<when test="searchType ==1">
						writer Like '%' || #{searchValue} || '%'
					</when>
					<when test="searchType == 2">
						grpcode Like'%' || #{searchValue} || '%'
					</when>
					<when test="searchType==3">
						comm Like '%' || #{searchValue} || '%'
					</when>
				</choose>
			</where>
		</if>
		order by renum desc) a) where r_num between #{begin} and #{end}
	</select>
</mapper>